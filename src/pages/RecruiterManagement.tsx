import { useDynamicTitle } from "../hooks";

const RecruiterManagement: React.FC = () => {
  useDynamicTitle("Quản lý người tuyển dụng");
  return <div>RecruiterManagement</div>;
};

export default RecruiterManagement;
